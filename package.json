{
  "name": "i18n-helper",
  "version": "1.0.0",
  "description": "A i18n help page to verify every strings on project",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run rollup\" \"http-server\"",
    "rollup": "rollup index.js --file i18n-helper.js --format iife --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Paulo Ferreira",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "concurrently": "^4.1.0",
    "http-server": "^0.11.1",
    "rollup": "^1.1.2"
  }
}
